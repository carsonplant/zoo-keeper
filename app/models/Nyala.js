export default class Nyala {
  constructor(name, color, gender, antlers, height, weight, lifeStage, birthPlace) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.antlers = antlers;
    this.height = height;
    this.weight = weight;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}
export default class Yak {
  constructor(name, color, gender, horns, height, weight, lifeStage, birthPlace, ) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.horns = horns;
    this.height = height;
    this.weight = weight;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}
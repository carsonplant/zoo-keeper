export default class Elephant {
  constructor(name, type, color, tusks, trunk, gender, height, weight, lifeStage, birthPlace) {
    this.name = name;
    this.type = type;
    this.color = color;
    this.tusks = tusks;
    this.trunk = trunk;
    this.gender = gender;
    this.height = height;
    this.weight = weight;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}
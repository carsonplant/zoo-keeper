export default class Walrus {
  constructor(name, type, color, tusks, gender, height, weight, lifeStage, birthPlace) {
    this.name = name;
    this.type = type;
    this.color = color;
    this.tusks = tusks;
    this.gender = gender;
    this.height = height;
    this.weight = weight;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}
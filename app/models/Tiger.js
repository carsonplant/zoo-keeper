export default class Tiger {
  constructor(name, type, color, gender, tail, height, weight, lifeStage, birthPlace) {
    this.name = name;
    this.type = type;
    this.color = color;
    this.gender = gender;
    this.tail = tail;
    this.height = height;
    this.weight = weight;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}
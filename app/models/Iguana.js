export default class Iguana {
  constructor(name, type, color, spikes, tail, gender, height, weight, length, lifeStage, birthPlace) {
    this.name = name;
    this.type = type;
    this.color = color;
    this.spikes = spikes;
    this.tail = tail;
    this.gender = gender;
    this.height = height;
    this.weight = weight;
    this.length = length;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}
export default class Flamingo {
  constructor(name, color, gender, height, weight, lifeStage, birthPlace) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.height = height;
    this.weight = weight;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}

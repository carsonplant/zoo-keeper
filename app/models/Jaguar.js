export default class Jaguar {
  constructor(name, color, gender, tail, height, weight, lifeStage, birthPlace) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.tail = tail;
    this.height = height;
    this.weight = weight;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}
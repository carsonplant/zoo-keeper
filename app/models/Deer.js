export default class Deer {
  constructor(name, type, gender, antlers, height, weight, lifeStage, birthPlace) {
    this.name = name;
    this.type = type;
    this.gender = gender;
    this.antlers = antlers;
    this.height = height;
    this.weight = weight;
    this.lifeStage = lifeStage;
    this.birthPlace = birthPlace;
  }
}